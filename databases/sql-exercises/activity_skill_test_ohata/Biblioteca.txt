CREATE TABLE TAB_LIVRO(
ID INT,
  TITULO VARCHAR(30),
    QTD_PAGINAS INT,
       ACABAMENTO VARCHAR (30),
           EDITORA VARCHAR(30)
);
CREATE TABLE TAB_AUTOR(
ID INT,  
  NOME VARCHAR(30),
    NACIONALIDADE VARCHAR(30),
      DATA_NASCIMENTO DATE,
          EMAIL VARCHAR(30)
);
INSERT INTO TAB_LIVRO (ID, TITULO, QTD_PAGINAS, ACABAMENTO, EDITORA) VALUES ('1', 'Kotlin em ação', 397, 'normal', 'Novatec');
INSERT INTO TAB_AUTOR (ID, NOME, NACIONALIDADE, DATA_NASCIMENTO, EMAIL) VALUES ('1', 'Rodrigo Martins', 'BRASILEIRO', '2001-04-27', 'rleite071@gmail.com');

SELECT * FROM TAB_AUTOR
SELECT * FROM TAB_LIVRO